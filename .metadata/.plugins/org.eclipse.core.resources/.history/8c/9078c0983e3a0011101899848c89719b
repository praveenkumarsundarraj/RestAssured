package reusableMethods;

import io.restassured.path.json.JsonPath;

public class ParseJson {

	public void getvalue(String response, String searchString)
	{
		JsonPath js = new JsonPath(response);
		js.getString(searchString);
	}
}
